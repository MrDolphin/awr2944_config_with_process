# AWR2944 首次实机验收记录

每次试验复制本文件，填写实际值，并把对应的 capture 目录、配置哈希和照片/视频一同归档。

## 1. 环境与版本

| 项目 | 实际值 |
| --- | --- |
| 日期、地点、天气/海况 | |
| 船体/码头状态 | |
| 雷达安装位置、朝向、高度 | |
| 供电电压与电源方案 | |
| 树莓派型号、系统版本 | |
| Git commit | |
| 配置文件名与 SHA-256 | |
| cfg/data 串口映射 | |

## 2. 启动验收

执行：

```bash
python3 tools/deployment_preflight.py --config Config/<profile>.cfg --require-ports
sudo systemctl start radar
journalctl -u radar -f
```

| 判定项 | 通过条件 | 结果 |
| --- | --- | --- |
| 预检 | 无 `FAIL` | |
| 服务启动 | systemd 为 active/running | |
| 网页健康状态 | 30 秒内从“等待雷达帧”变为“数据流正常” | |
| 数据口重连 | 拔插后 30 秒内恢复“数据流正常” | |

## 3. 采集质量验收

分别录制空场、静态已知物体、缓慢运动目标各不少于 60 秒。

| 场景 | capture 目录 | 帧数 | dropped_frames | writer_error | 结果 |
| --- | --- | ---: | ---: | --- | --- |
| 空场 | | | | | |
| 静态目标 | | | | | |
| 运动目标 | | | | | |

通过条件：`writer_error` 为空；`dropped_frames=0`；离线回放可看到空帧和目标帧；capture 的配置快照与现场下发配置一致。

## 4. 点云意义与精度记录

本阶段不以“点数多”作为通过条件。对每个场景记录：目标类型、实测距离（激光测距仪）、雷达距离估计、横向偏差、连续可见帧比例和误检说明。IMU/GNSS 到位后再补船体姿态和定位真值。

| 目标 | 真值距离(m) | 雷达距离(m) | 误差(m) | 连续可见帧比例 | 备注 |
| --- | ---: | ---: | ---: | ---: | --- |
| | | | | | |

## 5. 结论与异常

- 是否允许进入下一轮海上场景测试：
- 未通过项及日志/capture 链接：
- 下一次应调整的配置或安装项：
