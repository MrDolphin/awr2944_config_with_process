# Radar-only default. Add --enable-camera only after camera-only acceptance.
RADAR_CAMERA_ARGS=
RADAR_CAMERA_CONFIG=/home/pi/camera_web_fusion/tools/camera/camera_config.cfg
RADAR_CAMERA_HTTP_PORT=8081
