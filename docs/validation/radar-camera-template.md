# 雷达—相机融合现场验收记录模板

复制本文件为 `radar-camera-YYYYMMDD-HHMM.md`，填写每一门的实测值和证据路径。初始状态均为“未运行”；只有完成该门的实际操作并留存证据后才能改为“通过”或“失败”。本模板不含现场实测结果。原始会话、JPEG、日志和视频存放在 Git 仓库之外。

## 试验身份与证据

| 项目 | 现场记录 |
| --- | --- |
| 时间、地点、操作员与见证人 | 待填写 |
| Pi 型号、系统与供电 | 待填写 |
| Pi 检出目录、分支与 `git rev-parse HEAD` | 待填写 |
| `git status --short` | 待填写 |
| 雷达型号、供电、cfg/data 串口映射 | 待填写 |
| 相机稳定 `/dev/v4l/by-id/...` 路径 | 待填写 |
| 相机采集模式与 `v4l2-ctl --list-formats-ext` 证据 | 待填写 |
| 雷达 cfg 文件名、SHA-256 与下发记录 | 待填写 |
| 相机 cfg 文件名、SHA-256 | 待填写 |
| 标定 JSON 文件名、SHA-256 与安装模式 | 待填写；未标定时写“未加载” |
| 服务配置 `/etc/default/radar-camera`（隐去敏感内容） | 待填写 |
| systemd 实际 `ExecStart`、服务状态、最近日志 | 待填写 |
| 原始会话、截图、日志、验收 JSON 的保存位置 | 待填写 |

时钟口径：雷达帧与相机帧目前按 **Pi 接收时的单调时钟** 匹配；这不代表雷达射频采样时间或硬件触发同步。空间投影、目标分类和目标确认各自需要独立证据。

## 0. PC 离线软件检查

| 检查 | 命令 / 证据 | 结果 |
| --- | --- | --- |
| 全量单元测试 | `python -m unittest discover -s test -p 'test_*.py'`；填写数量和跳过项 | 未运行 |
| 网页本机模拟 | 记录浏览器、Playwright 版本与测试结果 | 未运行 |
| 部署脚本语法与 Git 差异 | `bash -n deploy/setup_rpi.sh`；`git diff --check` | 未运行 |

以上只证明本地软件状态，不能替代以下 Pi 和硬件门禁。

## 1. 雷达默认模式与受管部署

部署前先保存旧 `radar.service` 的 `systemctl cat`、进程命令和状态。记录受管检出中的提交，再按 [部署检查表](../../deploy/DEPLOYMENT_CHECKLIST.md) 更新。此门保持相机功能关闭。

| 判定项 | 通过条件 | 实测 / 证据 | 状态 |
| --- | --- | --- | --- |
| 服务启动与网页 | `8765` 监听，已有雷达状态正常显示 | 待填写 | 未运行 |
| 雷达串口和点云 | 行为与更新前基线一致 | 待填写 | 未运行 |
| 相机未启动 | 无 FFmpeg 采集进程 | 待填写 | 未运行 |
| 云台不动 | 无电机命令和非预期运动 | 待填写 | 未运行 |

## 2. 相机单独运行 30 分钟

已确认设备支持 `1280×720 MJPEG 30 FPS` 后，记录开始、10、20、30 分钟的温度、内存、帧率、进程状态和错误日志。记录 PC 对相机 HTTP 最新帧接口的访问结果。

| 时间 | CPU 温度 | 可用内存 / 进程 RSS | 实测 FPS | FFmpeg 重启次数 | HTTP / 日志证据 |
| --- | ---: | ---: | ---: | ---: | --- |
| 0 分钟 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 10 分钟 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 20 分钟 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 30 分钟 | 待填 | 待填 | 待填 | 待填 | 待填 |

判定：帧率 ≥25 FPS，无不支持模式回退、异常 FFmpeg 重启或持续内存增长；PC 能访问 HTTP 最新帧。结果：**未运行**。

## 3. 雷达与相机静态联合运行 10 分钟

云台保持静止，已知目标同时处于雷达和相机视野。记录相机启用前后的雷达帧率与丢帧情况，避免把本来存在的丢帧归因于相机。

| 判定项 | 通过条件 | 实测 / 证据 | 状态 |
| --- | --- | --- | --- |
| 点云持续输出 | 相机负载不导致雷达服务故障 | 待填写 | 未运行 |
| 相机帧率 | ≥25 FPS | 待填写 | 未运行 |
| 匹配比例 | ≥95% | 待填写 | 未运行 |
| 绝对接收时差 p95 | ≤50 ms | 待填写 | 未运行 |
| 雷达丢帧变化 | 无可归因于相机负载的帧损失 | 待填写 | 未运行 |
| 相机断开和恢复 | 雷达继续工作；状态转 `unavailable`；记录恢复是否需重启 | 待填写 | 未运行 |

## 4. 60 秒同步记录与十帧重放

| 项目 | 现场记录 |
| --- | --- |
| 记录目录、时长与复制到 PC 的位置 | 待填写 |
| `session_metadata.json` 中 Git、配置哈希、时钟基准和阈值 | 待填写 |
| `python3 radar_camera_session_validation.py <会话目录> > session-validation.json` 退出码与 JSON 路径 | 待填写 |
| 文件引用完整性、匹配比例、绝对偏差 p95 | 待填写 |
| `replay_samples` 中最多十组雷达帧和 JPEG 的逐组目视重放结果 | 待填写；逐组记录异常 |

验证器输出的 `replay_samples` 只是待重放清单；只有逐组打开雷达行和图片、核对目标与时间关系后，才能将本门记为通过。结果：**未运行**。

## 5. 静态标定与独立验证

| 判定项 | 通过条件 | 实测 / 证据 | 状态 |
| --- | --- | --- | --- |
| 相机内参 RMS | ≤1.5 px | 待填写 | 未运行 |
| 独立验证点误差中位数 | ≤8 px | 待填写 | 未运行 |
| 独立验证点误差 p95 | ≤20 px | 待填写 | 未运行 |
| 坐标系、安装模式、像素分辨率 | 与实际安装一致 | 待填写 | 未运行 |

记录标定集与独立验证集的来源、目标关联方法、外参文件及 SHA-256。不能用参与拟合的样本替代独立验证。

## 6. 操作员监护的扇区扫描

开始前记录线缆走线、机械限位、急停、周边安全和允许扇区。实际运动只能由现场操作员明确触发。

| 判定项 | 通过条件 | 实测 / 证据 | 状态 |
| --- | --- | --- | --- |
| 实测姿态年龄 p95 | ≤50 ms | 待填写 | 未运行 |
| 过期姿态叠加 | 姿态年龄 >100 ms 时无投影 | 待填写 | 未运行 |
| 运动中独立标靶误差 p95 | ≤30 px | 待填写 | 未运行 |
| 线缆、限位、异常重启 | 无缠绕、碰撞或失控 | 待填写 | 未运行 |
| 停止命令与急停 | 两者均已现场演示 | 待填写 | 未运行 |

## 偏差、回退与结论

| 项目 | 记录 |
| --- | --- |
| 未通过项和原始日志位置 | 待填写 |
| 偏离计划的操作及原因 | 待填写 |
| 回退动作、旧服务状态与数据保留位置 | 待填写 |
| 本次是否允许进入下一门 | 待填写 |
| 操作员和见证人签署 / 日期 | 待填写 |

最终结论：**待现场验收**。每门分别标记“未运行 / 通过 / 失败 / 有偏差”，不得以本地测试结果填充实机测量栏。
