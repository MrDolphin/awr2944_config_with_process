# TI AWR2944 算法优化策略

## 1. 优化目标

结合当前项目方向，算法侧后续优化不再以“远距离大范围扫描”为优先，而是围绕以下目标推进：

- 提升近距离目标检测稳定性
- 降低靠泊场景下的误报和漏报
- 提升低速、小目标和贴边目标的可识别性
- 提升复杂反射环境下的轨迹连续性
- 为辅助靠泊场景形成可重复调参的数据闭环

---

## 2. 当前算法基础

目前已具备的算法基础包括：

- 基础点云 TLV 解析
- 前端 DBSCAN 聚类与软件目标追踪
- 基础速度过滤与轨迹平滑
- CFAR 阈值调整与调试输出增强
- 数据录制与离线回放基础能力

这意味着当前系统已经具备继续做算法专项优化的前提，但仍缺少面向靠泊场景的定向调优。

---

## 3. 后续算法优化主线

### 3.1 检测层优化

目标是提升雷达在近距离、低速和复杂背景中的目标检出质量。

重点包括：

- 继续优化 `CFAR` 阈值策略，区分陆地模式、内海河道模式和靠泊模式
- 增加距离门限、速度门限和角度门限联动过滤
- 针对岸边、码头、船体固定结构建立静态杂波抑制策略
- 针对空旷环境误检多个目标的问题，建立独立问题样本库并分析触发条件

### 3.2 聚类与跟踪层优化

目标是让目标轨迹在靠泊场景下更稳定、更符合真实运动状态。

重点包括：

- 继续优化 DBSCAN 聚类半径与最小点数参数
- 区分静态障碍物、低速目标和动态目标的跟踪逻辑
- 优化轨迹生成的稳定性，减少目标 ID 跳变和轨迹断裂
- 针对低速靠近、平行移动、贴边移动等典型靠泊动作做专项测试

### 3.3 场景化模式优化

目标是把算法参数从“单一默认值”推进为“按场景切换”。

建议先建立三类模式：

- `调试模式`：尽量保留更多原始信息，便于排查问题
- `内海河道模式`：优先降低岸边和固定结构反射带来的误检
- `辅助靠泊模式`：优先保证近距离低速目标稳定检测和轨迹连续性

### 3.4 离线分析与回放优化

目标是让算法改动建立在真实数据基础上，而不是只靠现场观察。

重点包括：

- 每次测试同步保存配置、原始数据、截图和问题标记
- 建立典型问题数据集：误检、漏检、轨迹抖动、目标丢失
- 对同一组数据重复回放，验证参数调整前后的效果差异
- 为后续 UKF、扩展卡尔曼滤波或更稳的目标预测方法预留接口

---

## 4. 分阶段算法计划

## 第一阶段：误检与基础稳定性优化

目标：

- 优先解决空旷环境误检多个目标的问题
- 提升近距离低速目标的稳定检测效果

主要工作：

- 建立误检样本记录
- 调整 CFAR、速度门限和距离过滤参数
- 对典型误检场景做重复回放验证

## 第二阶段：轨迹连续性与目标分类优化

目标：

- 提升靠泊场景下的轨迹连续性和目标区分能力

主要工作：

- 优化 DBSCAN 聚类参数
- 优化目标关联与轨迹延续逻辑
- 增加静态/动态目标区分策略

## 第三阶段：场景模式化与专项靠泊优化

目标：

- 建立适配不同环境的参数模板
- 形成辅助靠泊专项参数集

主要工作：

- 建立调试模式、内海河道模式、靠泊模式
- 针对低速接近、障碍物靠近、岸边反射等场景做专项调优

## 第四阶段：高级算法预研

目标：

- 为后续高阶目标预测和更稳定跟踪做储备

主要工作：

- 评估 UKF / EKF 的接入价值
- 评估多帧融合和轨迹预测方法
- 为 BP-MIMO、beamforming 后续接入提供算法验证接口

---

## 5. 当前建议

算法侧近期最值得优先推进的，不是直接上更复杂模型，而是先把以下三件事做好：

1. 把误检和漏检问题用真实数据固定下来
2. 把近距离低速目标的稳定检测做扎实
3. 把参数从“单一默认值”变成“按场景切换”

如果这三步完成，后续无论是做辅助靠泊、模式扩展，还是更高阶跟踪算法，都会更稳。
