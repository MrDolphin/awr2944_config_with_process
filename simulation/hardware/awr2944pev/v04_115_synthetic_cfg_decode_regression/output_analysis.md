# V0.4.115 当前 CFG 全尺寸合成 IQ 回归

本阶段只验证解码链路，不代表硬件测量。

输入合成 IQ：`synthetic_capture.bin`；期望形状：`[64, 656, 4]`；实际形状：`[64, 656, 4]`；TDM 顺序：`[0, 2, 3, 1]`。

合成文件明确标记为 `synthetic_only=true`，不能用于 AWR2944P 实测 AoA、探测距离或海杂波结论。真实 capture.bin 到来后，应替换输入并保留独立结果目录。
