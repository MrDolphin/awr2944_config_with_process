# V0.4.121 CFAR 与微元物理代理联表

本阶段将 V0.45 的合成 CFAR 参数扫描结果与 V0.120 的波束掩膜微元统计按海况 case_id 关联。

## 如何分析

先在同一海况内比较不同 CFAR 场景，判断点数变化是否由门限/训练窗造成；再固定 `pfa1e3_train4`，横向比较海况与 3 dB 微元数、散射代理、Doppler 代理。图中点数/帧是 CFAR 输出候选，不是实测虚警率。

## 证据边界

两侧均为合成模型：CFAR 输入来自 V0.43 合成 Range-Doppler，物理字段来自 V0.42/V0.120 高度场推导。联表只用于解释变量关系和发现下一步实验设计，不代表因果关系或 AWR2944P 性能。
