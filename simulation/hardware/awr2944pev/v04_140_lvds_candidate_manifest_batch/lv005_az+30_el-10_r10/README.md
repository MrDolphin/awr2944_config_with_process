# lv005_az+30_el-10_r10

目标：方位 30.0°，俯仰 -10.0°，距离 10.0 m。

这是加入 `lvdsStreamCfg -1 0 1 0` 的非权威候选 CFG。先由操作员确认 TI SDK/固件兼容、DCA1000 连接和安全停机条件，再放入 capture.bin、DCA 配置、TI 校准文件和姿态记录。

当前目录是采集模板，不代表已下发配置或已完成实测。
